name: Test TAG Workflow

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  context:
    runs-on: actions-playground
    steps:
      - run: ${{ github }}
  echo:
    if: github.ref_type == 'tag'
    runs-on: actions-playground
    steps:
      - run: echo "This is a PR branch that should be ignored"